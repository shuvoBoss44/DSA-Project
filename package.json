{
  "name": "search-engine-gui",
  "version": "1.0.0",
  "description": "Premium Liquid Glass GUI for C++ Search Engine",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "g++ -std=c++17 -O3 main.cpp -o main && cd frontend && npm install && npm run build",
    "postinstall": "g++ -std=c++17 -O3 main.cpp -o main && cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "nodemon": "^3.1.14"
  }
}
